{
  "_comment": [
    "SHIPPED WITH THE PLUGIN, AND THE ONLY COPY. The minimum runner version the",
    "three commands accept.",
    "",
    "Resolved, and not typed: minimum is the repository's RUNNER_VERSION, and",
    "resolved_by is the commit that set RUNNER_VERSION to that value, found by",
    "git log -S on the constant in config.js, whichever commit that is. The two are",
    "one fact in two fields and move in the same commit. The threshold was recorded",
    "null until the first merge that moved RUNNER_VERSION, which resolved it.",
    "",
    "THIS FILE IS AC-5'S SUBJECT (spec 028 A-028-17). The gate resolves the path from",
    "door.mjs's own `const GUARD` line and asserts these fields here: minimum against",
    "the repository RUNNER_VERSION, resolved_by against git ancestry, and the anchor",
    "against decision 5's no-spec-name rule. It also asserts that exactly one file of",
    "this name is tracked, so there is no second copy to edit instead.",
    "",
    "Until A-028-17 this header claimed to be `Held identical to",
    "specs/028-claude-code-plugin/fixtures/version-guard.json by spec 028 AC-5`. No",
    "line of AC-5 held that. The criterion read the fixture and the door read this",
    "file. That fixture is deleted and the claim is gone with it."
  ],
  "minimum": "0.10.1",
  "resolved_by": "975d475",
  "resolves_at": "the commit that sets RUNNER_VERSION to minimum",
  "compare": "semver-gte"
}
