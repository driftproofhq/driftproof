# Full-plugin functional follow-up

Source is unchanged addyosmani/agent-skills at the commit in provenance.json. All plugin files, skills, references, agents and default SessionStart hook are present. Only a new self-authored codex-full-evals directory is added in the disposable clone. Optional simplify-ignore hooks are not enabled because they are not part of the default plugin.

Three new practical tasks exercise the same three skills: inspect a defective working-tree change and write a review; create actual atomic Git commits; create an ADR following an existing reStructuredText convention. Explicit skill requests are conditional on availability so no-plugin baselines remain meaningful. This tests execution with explicit invocation, not natural-language discovery rates. Different tasks from the earlier text-only comparison; scores are not comparable to that report.

Native uses official Claude Code 2.1.272 with the whole plugin, self-authored scaffolds, local Bash/Write/Edit and read/skill/agent tools. No external services or publishing. Three runs per arm are planned, with full temporary-workspace retention. Deterministic native graders are coarse presence/tool checks; independent post-session verification inspects actual Git history, unchanged files, source and output contents. Passing regexes alone is not sufficient for the final verdict. No model judges are used. Target and utility models explicitly Opus 5. Budget $20 list-price equivalent for the native suite; per-run timeout 420s and turn cap 35. These are functional cases, not comprehensive reliability estimates.

Driftproof 0.10.1 is inspected and attempted against the full plugin using its stock CLI. Its standard run accepts a skill directory and injects SKILL.md; full-plugin loading, fixture reset per generation, and final-workspace verification are not documented features. Unsupported full-plugin execution will be reported honestly, not replaced by a custom wrapper producing apparently official TESTED receipts. No imported or stub receipt will be presented as a live full-plugin result.

Authentication is checked before live runs through Claude.ai subscription; no API key. Scratch work only; no user-scope plugin installation, upstream modifications, or public posting. Logs and resulting workspaces are copied into the final local bundle, then only test-created temporary directories are removed.
